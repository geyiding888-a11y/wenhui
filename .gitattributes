# 换行符规则。**这个文件不是可有可无的**。
#
# 为什么必须有：项目的三个双击启动文件（.bat）**必须是 CRLF 换行**。
# 用 LF 的话，Windows 的 cmd.exe 会把两行读成一行，症状是中文行消失、
# 下一行的尾巴被当成命令执行（详见 read.md 第七节）。
# 这个坑我们真踩过，修了一整轮。
#
# 而"换行符怎么处理"这件事，默认是靠**每台电脑上的 git 配置**
# （core.autocrlf）决定的——那是本机设置，**不会跟着仓库走**。
# 换台电脑克隆下来，或者谁改了一下全局配置，.bat 就可能变回 LF 又坏掉。
#
# .gitattributes 写在仓库里，会跟着仓库走：谁克隆都是这套规则。
# 所以它是那个 bug 的第二道保险（第一道是 tests/test_launchers.py）。

# 默认：所有文本文件在仓库里统一存成 LF
* text=auto eol=lf

# Windows 批处理：检出时必须是 CRLF
*.bat text eol=crlf

# Mac 的 shell 脚本：必须是 LF（CRLF 会报 bad interpreter: ^M）
*.command text eol=lf

# Excel 是二进制，不许做任何换行符转换
*.xlsx binary
*.xls  binary

# 锁文件体积大、且是自动生成的，别做转换省得噪声
uv.lock text eol=lf
